import React from 'react'

const Home = () => {
  return (
    <div className='h-screen w-full flex items-center justify-center'>Home page (first page the user sees when entering the application for the first time)</div>
  )
}

export default Home;